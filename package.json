{
  "name": "sitecussy",
  "version": "0.1.0",
  "private": true,
  "description": "Site VTT Cussy-en-Morvan — monorepo (Strapi backend + Nuxt frontend)",
  "scripts": {
    "prepare": "husky",
    "lint:frontend": "npm --prefix frontend run lint",
    "format:frontend": "npm --prefix frontend run format"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "frontend/**/*.{vue,ts,js,mjs,json,md,css}": [
      "prettier --write"
    ]
  }
}
